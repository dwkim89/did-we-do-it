interface:
  display_name: "Weekly Beamer Builder"
  short_description: "Build weekly status decks with clear differentials"
  default_prompt: "Use $build-weekly-beamer to prepare a roughly 15-frame weekly status deck with last-week context, updated evidence, supporting backup, and a verified PDF."
