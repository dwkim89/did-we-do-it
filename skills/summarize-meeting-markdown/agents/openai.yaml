interface:
  display_name: "Meeting Markdown Summary"
  short_description: "Normalize an existing summary or summarize a raw transcript"
  default_prompt: "Use $summarize-meeting-markdown to prefer an existing meeting summary, or summarize the raw transcript when needed, and produce reviewed dated Markdown."
